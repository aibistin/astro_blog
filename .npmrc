enable-pre-post-scripts=true
# Hoist @tailwindcss/oxide and its platform-specific binary packages to root
# node_modules so pnpm's strict isolation doesn't break native require() calls
public-hoist-pattern[]=@tailwindcss/oxide*